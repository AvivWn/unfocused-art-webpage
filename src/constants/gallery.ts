/** Minimum gap between paintings on the grid, in grid units (1 = 1 cm). */
export const GALLERY_PACK_GAP = 2;

/** Smallest tile width on the wall, in cm (1 grid unit = 1 cm). */
export const GALLERY_MIN_TILE_WIDTH_CM = 9;
